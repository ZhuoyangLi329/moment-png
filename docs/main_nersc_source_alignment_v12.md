# GitHub main / NERSC source alignment v12

After the final release-gate update, all tracked Python source files were compared against the NERSC runtime tree using SHA256 byte equality. The recorded main commit has matching source hashes with zero mismatches.

Machine-readable record: `results/main_nersc_source_alignment_v12.json`.
