# GitHub main / NERSC source alignment v10

After the final release-gate source update, all tracked Python sources were compared against the NERSC runtime tree using SHA256 byte equality. The recorded main commit has 63 source files, with 63 matches and zero mismatches.

Machine-readable record: `results/main_nersc_source_alignment_v10.json`.
