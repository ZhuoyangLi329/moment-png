# Run provenance v39

This ledger records the training-only CIC/shot convention scan and its held-out test alongside the frozen protocol and the successful NERSC batch resource record. The scan was a lightweight interactive diagnostic, so its plotting run has `slurm_job_id: null`; the mode-power production-like batch remains job `58089819` with complete Slurm resources.

The `CIC both` convention fits `b1=2.7413` and `P_shot=5467.8` from the fiducial training power, then gives Hartlap χ²/dof 1.96, 8.03, and 4.34 for fiducial, LC_m, and LC_p. The result is an improvement diagnostic, not a production promotion.
