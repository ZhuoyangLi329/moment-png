# GitHub main / NERSC source alignment v16

The final canonical protocol guard and all tracked Python sources were synchronized before this check. Every tracked `src/*.py` file in GitHub `main` matches the NERSC runtime tree byte-for-byte by SHA256, with zero mismatches.

Machine-readable record: `results/main_nersc_source_alignment_v16.json`.
