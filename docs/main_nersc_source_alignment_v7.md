# GitHub main / NERSC source alignment v7

After adding the full-lattice μ2 identity validator, the clean GitHub `main` checkout and NERSC runtime tree were compared again file by file with SHA256 byte equality. At commit `f86c25b148273fc6920193225c1362ffb6db33b2`, all 62 tracked `src/*.py` files match the NERSC tree, with zero mismatches.

The machine-readable record is `results/main_nersc_source_alignment_v7.json`.
