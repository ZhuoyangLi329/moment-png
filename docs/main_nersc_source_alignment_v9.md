# GitHub main / NERSC source alignment v9

After adding the fundamental-mode loop audit, the clean GitHub `main` checkout and NERSC runtime source tree were compared with SHA256 byte equality. At the recorded commit, all 63 tracked `src/*.py` files match with zero mismatches.

Machine-readable record: `results/main_nersc_source_alignment_v9.json`.
