# GitHub main / NERSC source alignment v18

The held-out μ2 PNG response covariance validator was synchronized before this check. All tracked Python sources in GitHub `main` match the NERSC runtime tree byte-for-byte by SHA256, with zero mismatches.

Machine-readable record: `results/main_nersc_source_alignment_v18.json`.
