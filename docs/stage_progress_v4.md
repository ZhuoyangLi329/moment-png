# Stage progress v4

The consolidated stage ledger now records the corrected full-lattice Gaussian μ2 identity audit. Using the complete mode power from each held-out realization and the ensemble-centered Wick definition gives machine-precision agreement with the stored Gaussian component; this closes the estimator identity check.

Stage 7 remains `BASELINE_OPEN` because connected halo four-point, PNG response, contact, and clustered terms are still unresolved. Stage 6 remains `ASSUMPTION_ONLY` because the strict calibration scan found no matching independent/disjoint b_phi source.

Machine-readable ledger: `results/stage_progress_v4.json`.
