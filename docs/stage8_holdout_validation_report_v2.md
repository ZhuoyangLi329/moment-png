# Stage 8 held-out validation report v2

This version extends the canonical 30-realization held-out report with the disjoint fiducial `real100`--`real199` (b_1) audit, the delta-method covariance for (b_1,\delta_c,p,b_\phi), and both local and literature (b_\phi) scope checks.

The disjoint Gaussian (b_1) audit passes at (k_{\max}=0.03) with (\Delta b_1=-0.002314), while the derived universal branch remains assumption-only with (b_\phi=5.60204\pm0.16873). The report status remains `REJECTED_DIAGNOSTIC`: the joint and PNG-response residuals are retained, and no (b_\phi), empirical polynomial or held-out response is promoted into a production mean.
