# GitHub main / NERSC source alignment v14

The final release-gate source update and the signed loop-CIC validator were synchronized before this check. Every tracked Python source in GitHub `main` matches the NERSC runtime tree byte-for-byte by SHA256, with zero mismatches.

Machine-readable record: `results/main_nersc_source_alignment_v14.json`.
