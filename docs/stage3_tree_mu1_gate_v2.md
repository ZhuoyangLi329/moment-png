# Stage 3 tree μ1 gate v2

The canonical exact lattice and shell window conventions are verified with the modewise identity. The universal (p=1.12) tree model is rejected by the held-out absolute (f_{NL}=0) and PNG-node comparisons, while training-only measured-(P_h) and response-calibrated (b_\phi=3.76365) curves are retained as diagnostics.

The diagnostic improvement uses only real000--real069 for calibration and real070--real099 for evaluation. It cannot be promoted because the (b_\phi) scope audit found no independent source matching the frozen FoF selection. The gate therefore remains `BLOCKED`, with all ratios, pulls, covariance-aware statistics and coverage retained in the source JSONs.
