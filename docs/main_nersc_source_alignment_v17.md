# GitHub main / NERSC source alignment v17

The latest overlay plotting source was synchronized before this check. All 67 tracked Python source files in GitHub `main` match the NERSC runtime tree byte-for-byte by SHA256, with zero mismatches.

Machine-readable record: `results/main_nersc_source_alignment_v17.json`.
