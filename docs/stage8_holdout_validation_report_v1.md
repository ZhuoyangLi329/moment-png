# Stage 8 held-out validation report

This report uses the canonical v1 config and manifest, with the exact real070--real099 held-out split and all 14 frozen shells. It records leakage-free diagnostics for universal tree μ1, training-only P_h μ1, training-only P_h Gaussian μ2, joint μ1+μ2G shrinkage scans, and the paired held-out μ2 PNG response.

The report status is `REJECTED_DIAGNOSTIC`, preserving every failure rather than promoting a diagnostic curve. Its source hashes and policy fields prove that no held-out response or empirical polynomial entered a production mean.
