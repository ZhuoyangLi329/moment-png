# Disjoint (b_1) consistency audit

The independent fiducial block `real100`--`real199` gives (b_1=2.7317333) at the reference (k_{\max}=0.03\,h\,\mathrm{Mpc}^{-1}), compared with the frozen training calibration (b_1=2.7340475). The difference is (-0.0023142), below the predeclared absolute tolerance 0.02, so the Gaussian (b_1) consistency check passes.

Fits at larger (k_{\max}) are retained to expose nonlinear drift and are not used to select the production cut. This audit supports (b_1) input consistency only; it does not calibrate (b_\phi), (b_{\phi\delta}), or any PNG response.
