# GitHub main / NERSC source alignment v8

The source alignment was regenerated after the release-gate ledger update. At the recorded GitHub `main` commit, all 62 tracked Python source files match the NERSC runtime tree byte-for-byte by SHA256, with zero mismatches.

Machine-readable record: `results/main_nersc_source_alignment_v8.json`.
